// const express = require("express");
// const router = express.Router();
// const mongoose = require("mongoose");
// const  toId = mongoose.Types.ObjectId;

// const { Owner, Dog } = require("../modals/Shema");

// router.get("/seed", async (req, res) => {
//   dogs = [{ name: "Rover" }, { name: "Spot" }, { name: "Biff" }];

//   owner = [{ name: "john" }, { name: "steve" }, { name: "gokul" }];

//   const newDogs = await Dog.create(dogs);
//   const newOwners = await Owner.create(owner);

//   res.json({ newOwners, newDogs });
// });



// module.exports= router